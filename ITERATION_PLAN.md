# ScholarBrain 迭代计划

## 1. 项目定位

ScholarBrain 的目标不是做一个“上传 PDF 后调用大模型聊天”的普通 RAG Demo，而是做成一个可展示、可复现、可持续迭代的科研论文智能阅读系统：

- 可靠地解析科研 PDF，并恢复章节、公式符号和引用结构。
- 让用户能够沿着论文 Outline 定位、检索和追溯答案证据。
- 让 ResearchAgent 和 LibrarianAgent 在可信知识库上完成阅读与审计。
- 让解析、检索和回答质量都能被测试、量化和解释。
- 具备可部署、可观测、可恢复的工程闭环，而不只是本机可运行。

当前项目已经完成远程 MonkeyOCR 接入、Outline 规则恢复、Paper 模块第一阶段迁移和 Agent 框架化，下一步应从“功能能跑”转向“链路可靠、效果可信、产品可演示”。

## 2. 总体原则

1. **先保证知识库可靠，再扩展 Agent 数量。** PDF 解析和论文入库是所有上层能力的地基。
2. **先做可评测的 RAG，再考虑模型微调。** Prompt、检索、重排和工具链没有形成稳定基线前，不进行微调。
3. **失败必须可见、可恢复。** 任何外部模型、远程 GPU、网络或数据库错误都不能表现为“成功但结果为空”。
4. **规则与模型决策必须可审计。** Outline、检索结果和 Agent 回答都要能解释其来源。
5. **按业务所有权收口模块。** 新模块不再依赖旧 `w1_ai-domain` 和 `w1_ai-types`，但不为删除模块而机械搬文件。
6. **每个阶段都形成可演示成果。** 每一阶段都应有 API、测试、指标或界面结果，而不是只有内部重构。

## 3. 路线总览

| 阶段 | 主题 | 核心结果 | 优先级 |
|---|---|---|---|
| 阶段 0 | 工程基线与安全治理 | 测试可运行、样本入库、密钥和日志清理 | 已完成 |
| 阶段 1 | 论文摄取可靠性 | 状态机、失败审计、幂等、阶段重跑 | 已完成 |
| 阶段 2 | RAG 2.0 与评测 | 混合检索、重排、引用定位、稳定评测集 | 已完成 |
| 阶段 3 | Agent 可信回答与知识审计 | 回答证据链、关系置信度、可解释审计 | 已完成 |
| 阶段 4 | 产品化阅读体验 | Outline 阅读器、进度和失败处理、证据交互 | 已实现，待联调验收 |
| 阶段 5 | 架构收口与部署 | 删除旧模块、数据库迁移、Compose、CI | 中高 |
| 阶段 6 | WriterAgent 与模型优化 | 写作闭环、数据积累、必要时微调 | 后置 |

---

## 4. 阶段 0：工程基线与安全治理

### 目标

让仓库拥有可信的测试入口和可提交的样本基线，避免编码、密钥、运行日志和外部服务干扰后续迭代。

### 已完成

- 清理文档中的真实密钥和实例连接信息，配置改用环境变量或本地忽略文件。
- 忽略运行日志、调试文件等本地产物，并移除已跟踪日志。
- 将 19 份 Outline Markdown 样本纳入测试资源管理。
- 将 Outline 测试改成不依赖开发者本机路径的单元测试。
- 将 MonkeyOCR、网络代理等外部测试与默认单元测试隔离。
- 更新 Surefire 配置，默认只执行稳定的 `*UnitTest`。
- 通过 `.mvn/jvm.config` 固定 Maven 测试进程为 UTF-8。
- `mvn test -pl w1_ai-app -am` 已由开发环境验证通过。

### 阶段结论

阶段 0 已收工。后续修改必须继续保持：默认测试不依赖 Redis、PostgreSQL、外部 API 或远程 MonkeyOCR。

---

## 5. 阶段 1：论文摄取状态机、失败审计和阶段重跑（已实现，待环境验收）

### 5.1 要解决的问题

当前上传流程本质上仍是一段较长的同步方法：上传 PDF 后依次调用远程 MonkeyOCR、Outline 归一化、元数据抽取、数据库保存、Embedding 和 Librarian 审计。它存在以下风险：

- MonkeyOCR 或网络失败时，系统可能没有可查询的任务记录。
- 元数据抽取失败可能返回 `paperId = null`，调用方无法区分失败阶段。
- 失败后只能重新上传，可能重复消耗 GPU、Embedding 和 LLM 调用。
- 文件指纹生成和查重偏晚，重复论文可能已经完成昂贵 OCR。
- 应用重启后，内存中的异步任务无法恢复。
- 原始 Markdown、规范 Markdown 和归一化报告没有统一版本关联，难以复查结果。
- Paper 状态与摄取任务状态混在一起，论文尚未创建时无法记录失败。

这一阶段不是简单增加几个状态字符串，而是把论文上传改造成一个**持久化、幂等、可恢复、可审计的工作流**。

### 5.2 核心设计

新增独立的摄取任务 `PaperIngestJob`。任务在收到文件后立即创建，因此即使论文实体尚未生成，也始终能返回 `jobId`。

建议的业务状态：

```text
UPLOADED -> PARSING -> PARSED -> AUDITING -> READY
     |         |          |          |
     +---------+----------+----------+-> FAILED
```

业务状态用于展示整体进度；技术阶段用于精确定位失败：

```text
FILE_VALIDATION
DEDUPLICATION
MONKEY_OCR
STRUCTURE_NORMALIZATION
METADATA_EXTRACTION
PAPER_PERSISTENCE
SYMBOL_ENRICHMENT
CITATION_ENRICHMENT
EMBEDDING
LIBRARIAN_AUDIT
```

任务至少记录：

- `jobId`、可空的 `paperId`、原文件名和文件 SHA-256。
- 当前状态、当前阶段、失败阶段、错误码和可读错误信息。
- 尝试次数、创建/开始/结束时间、乐观锁版本。
- 解析器类型与版本、Outline 规则版本、配置版本。
- 原始 PDF、MonkeyOCR 原始结果、规范 Markdown、决策报告和元数据结果的位置。

### 5.3 API 行为

- 上传接口持久化文件和任务后，快速返回 `202 Accepted + jobId`。
- 状态查询接口返回进度、当前阶段、`paperId` 和失败原因。
- 重试接口从失败阶段继续，不默认重跑全部流程。
- 指定阶段重跑接口用于 Outline 规则升级、元数据重抽取或重新向量化。
- 全量重跑必须显式指定，避免无意中再次调用远程 GPU。

### 5.4 幂等与重跑规则

- 上传后先计算文件 SHA-256，在 MonkeyOCR 之前识别重复文件。
- 同一文件同一时刻最多存在一个活动摄取任务。
- 已有 MonkeyOCR 产物时，Outline 重跑不得再次请求 GPU。
- Embedding 失败重试时，不重新做 OCR、结构恢复和元数据抽取。
- 每个阶段独立提交事务，外部 HTTP 调用不包在长数据库事务中。
- 章节、符号、引用和论文关系采用替换或幂等写入，重跑不能产生重复数据。
- 应用重启后，未完成任务能够被恢复扫描器重新领取，而不是永久卡住。

### 5.5 实施顺序

1. 定义摄取任务、状态和阶段模型，增加数据库表及 Repository port。
2. 将上传接口改为“保存文件 + 创建任务 + 返回 jobId”。
3. 将现有长流程拆成可独立执行的阶段处理器。
4. 保存每一阶段的产物、版本和执行记录。
5. 实现失败审计、任务领取、超时任务恢复和并发保护。
6. 实现失败阶段重试、指定阶段重跑和全量重跑。
7. 接入 Librarian 异步审计，最终统一使用 `READY/FAILED`。
8. 增加任务查询 API 和最小化进度展示。

### 5.6 验收标准

- 每次合法上传都立即获得 `jobId`，不再出现“成功但 `paperId` 为空”。
- 在 MonkeyOCR、元数据、数据库、Embedding、引用增强任一阶段制造失败，都能查询到明确阶段和原因。
- 同一 PDF 重复上传能在 OCR 前识别，不重复消耗远程 GPU。
- Embedding 阶段失败后重试，MonkeyOCR 调用次数不增加。
- Outline 规则版本变化后，可直接基于原始 Markdown 重跑。
- 任一阶段重跑不会重复插入章节、符号、引用和关系数据。
- 应用在处理期间重启，任务不会丢失，并可继续或明确失败。
- 单元测试不依赖真实外部服务；集成测试使用 Stub/Fake 验证状态流转。

### 5.7 暂不包含

- 不引入 Kafka、RabbitMQ 等消息中间件；第一版使用数据库任务表、Spring 托管执行器和恢复扫描器即可。
- 不在本阶段升级检索算法。
- 不新增 Agent，不实现 WriterAgent，不进行模型微调。

---

## 6. 阶段 2：RAG 2.0、引用定位和可复现评测

### 当前实施进度

- 已新增结构化 `section_chunks` 索引，保留原始 Section/Outline 作为阅读结构。
- 已接入 MonkeyOCR `content_list` 页码、Heading Path、Chunk 向量和 PostgreSQL 全文索引。
- 已将检索升级为章节/符号/引用分通道召回，并使用 RRF 融合。
- 已提供 `vector/fulltext/hybrid` 三种可比较模式和首版版本化评测集。
- 待真实数据库回填后记录基线，再决定是否接入 Cross-Encoder 或 LLM reranker。

### 目标

解决“论文已经正确入库，但检索不一定能找到最有用证据”的问题，使回答质量建立在可验证的检索基线上。

### 主要工作

- 修复当前检索阈值计算后未真正参与过滤的问题。
- 将仅向量 Top-K 升级为真正的混合检索：向量召回 + PostgreSQL 全文/BM25 召回。
- 对章节、符号、引用使用分通道召回，再进行统一融合。
- 引入 RRF 或加权融合，并增加可替换的 Cross-Encoder/LLM reranker。
- 利用 Outline 路径、章节标题、父子章节和论文 ID 增加结构化检索信号。
- 返回 `paperId/sectionId/headingPath/page/quote/score`，为 Agent 证据引用打基础。
- 将评测样本从当前探索性小样本扩充为可版本管理的数据集，包含事实问答、跨章节问答、符号解释和引用追溯。
- 固定模型、Prompt、检索参数和数据集版本，输出可比较的 RAGAS 与检索指标。

### 核心指标

- Recall@K、MRR、nDCG：衡量证据是否被召回以及排序质量。
- Context Precision/Recall：衡量提供给模型的上下文质量。
- Faithfulness、Answer Relevancy：衡量最终回答质量。
- P50/P95 延迟和单次查询成本：衡量工程可用性。

### 验收标准

- “HybridRetriever” 名称与实现一致，不再只是向量搜索。
- 每个评测问题都拥有人工标注的目标章节或证据。
- 检索参数变化可以通过同一评测命令回归比较。
- README 只展示可复现的当前基准，不引用偶然的历史最高值。

---

## 7. 阶段 3：Agent 可信回答和 Librarian 知识审计

### 目标

让 Agent 的价值从“会调用工具”升级为“能给出可追溯证据、能说明判断依据、失败时不编造”。

### ResearchAgent

- 统一 Tool 注册与发现，逐步替换手写 `switch` 分发。
- 回答必须绑定章节证据，展示论文、章节路径和引用片段。
- 支持先定位 Outline、再读取目标章节、必要时追踪符号和引用的研究流程。
- 增加无充分证据时的拒答或不确定性表达。
- 对 Agent trajectory 做结构化日志，记录每一步 action、observation、耗时和 token。

当前 3A 范围先收敛为“可复查证据链”：仅输出本轮真实工具调用得到的论文、章节、Chunk、符号或引用来源，供读者自行回查；暂不使用规则或第二个模型验证最终结论是否被证据充分支持。

### LibrarianAgent

- 论文关系增加 `UNRELATED`，不再强制把所有论文归入某种关系。
- 每个关系包含置信度、支持证据、冲突证据和模型版本。
- 审计结果支持重新执行，并按版本替换，避免重复关系。
- 区分引用关系、主题相似、方法继承、结果支持和结果冲突。

### 验收标准

- ResearchAgent 的关键结论均可点击或查询到原始章节证据。
- 无证据问题不会生成看似确定的答案。
- Librarian 的每条关系都有置信度和至少一条证据；低置信结果可进入待审核状态。
- Agent 回归集能够验证工具选择、证据正确性和最终回答。

---

## 8. 阶段 4：产品化论文阅读体验

### 当前实施进度

- 新增独立 React/TypeScript 工作台 `w1_ai-web`，通过 Vite 代理连接 Java API。
- 在既有 Paper API 中补充结构化读取接口，聚合论文、Outline、章节、符号、引用、关系和最近摄取任务。
- 已实现 Outline 阅读、章节上下文、ResearchAgent SSE 回答及证据回跳。
- 已实现上传、阶段时间线、失败重试、指定阶段重跑和四类解析产物审阅。
- 已实现 Librarian 关系的已确认、待复核、无关审计历史分组展示。
- 已补齐 SSE 正常完成/异常关闭，避免前端连接长期悬挂。

### 目标

把后端能力变成面试现场可直观看懂、用户真正可以操作的科研阅读产品。

### 主要工作

- 上传后展示摄取时间线：上传、OCR、结构恢复、入库、增强、审计。
- 失败时展示原因和重试入口，不只弹出通用错误。
- 实现可折叠 Outline，点击章节加载原文、符号和引用。
- Agent 回答中的证据可跳转到对应章节并高亮原文。
- 提供论文间关系视图，展示支持、冲突、引用和相似关系。
- 增加解析产物审查页，对比原始 Markdown、规范 Markdown 和 Outline 决策报告。
- 对长任务、空状态、网络断开和外部 GPU 不在线提供完整交互状态。

### 验收标准

- 用户可以从上传一篇 PDF 开始，完成“观察进度 -> 浏览 Outline -> 提问 -> 查看证据 -> 查看关联论文”的完整演示。
- 远程 MonkeyOCR 不在线时，界面能够解释问题并允许恢复。
- 常见桌面分辨率下 Outline、正文和问答区域无重叠，长标题可正常显示。

---

## 9. 阶段 5：架构收口、可观测性和部署

### 架构收口

- 继续按业务所有权迁移旧 `w1_ai-domain` 中剩余类型。
- `w1_ai-paper`、ResearchAgent 和 LibrarianAgent 只依赖各自 API 与共享内核。
- 清理 `w1_ai-types`，统一异常、事件和工具返回类型。
- 旧模块无调用方后再删除，不进行一次性大爆炸式迁移。
- 明确 `api/domain/internal`：`api` 是跨模块契约，`domain` 是领域模型与规则，`internal` 是模块内部用例实现。

### 部署与运维

- 使用 Flyway 或 Liquibase 管理 PostgreSQL/pgvector 表结构版本。
- 提供 Java App、PostgreSQL/pgvector、Redis 的 Docker Compose。
- MonkeyOCR 作为独立 GPU 服务部署，通过配置选择远程 HTTP、SSH 隧道或回退解析器。
- 增加 `/health`、数据库、Redis、Embedding、MonkeyOCR 等依赖健康检查。
- 增加结构化日志、traceId/jobId、阶段耗时、失败率和外部调用耗时。
- 建立 CI：编译、单元测试、架构依赖检查、编码/密钥扫描。
- 提供一条命令启动本地依赖，以及一份从零部署文档。

### 验收标准

- `w1_ai-domain` 和 `w1_ai-types` 删除后全量编译、单元测试和集成测试通过。
- 新环境可根据文档完成配置和启动，不依赖开发者 IDE 私有设置。
- 数据库升级由迁移脚本完成，不靠手工执行散落 SQL。
- 关键链路能按 `jobId` 追踪，能统计每个摄取阶段的耗时和失败率。

---

## 10. 阶段 6：WriterAgent 和模型优化

### WriterAgent

WriterAgent 不作为当前主线。只有在论文库、检索和证据链稳定后，才实现：

- 根据研究问题和已有论文生成可编辑写作计划。
- 每个段落绑定来源证据，禁止生成无来源引用。
- 支持章节重写、引用格式管理和事实一致性检查。
- LibrarianAgent 对草稿进行冲突、重复和证据完整性审查。

### 模型微调的进入条件

满足以下条件后再评估 LoRA/QLoRA，而不是为了简历标签提前微调：

- 已积累稳定、合法、版本化的训练和验证数据。
- 已有 Prompt/RAG/重排基线，并能明确指出模型仍解决不了的问题。
- 目标任务有清晰指标，例如 Outline 候选判断、关系分类或工具选择。
- 4090 显存和推理成本能够支撑所选模型。
- 微调结果相较基线有可复现提升，而不是只展示训练过程。

优先考虑小而明确的任务微调，不建议第一步就在单张 4090 上部署或微调大型 DeepSeek 全量模型。项目价值应来自系统设计和真实指标，而不是“使用了某个模型”的名称。

---

## 11. 阶段依赖和推荐执行顺序

```text
阶段 0 工程基线
  -> 阶段 1 摄取可靠性
      -> 阶段 2 RAG 与评测
          -> 阶段 3 Agent 证据链
              -> 阶段 4 产品体验

阶段 1 至阶段 4 期间持续推进阶段 5 的小步架构收口；
阶段 6 必须等待阶段 2 和阶段 3 形成稳定数据与指标。
```

推荐不要同时开启超过两个主线。当前主线是 **阶段 4 产品化阅读体验联调验收**；通过完整演示链后进入阶段 5 的架构收口与部署。

## 12. 项目完成度判断

当以下场景可以稳定演示时，ScholarBrain 才算从课程项目进入有竞争力的秋招核心项目：

1. 上传任意主流科研 PDF，立即获得任务编号并观察处理进度。
2. 远程 OCR 或外部 API 失败后，能看到原因并从失败阶段恢复。
3. 论文进入 `READY` 后，Outline、符号、引用和向量索引均可查询。
4. ResearchAgent 基于正确章节回答，并给出可跳转的证据。
5. LibrarianAgent 对论文关系给出置信度和支持证据。
6. 一组版本化评测数据能够证明检索或 Agent 迭代确实提升了效果。
7. 新环境能够按文档部署，关键链路可观测，自动测试可重复运行。

这条路线的重点不是堆更多功能，而是把“远程 GPU 论文解析 + 结构化知识入库 + 可解释 Agent 阅读”做深。完成阶段 1 至阶段 3 后，项目已经具备很强的系统设计、AI 工程和效果评测叙事；阶段 4 和阶段 5 负责把它变成完整、可信的作品。
